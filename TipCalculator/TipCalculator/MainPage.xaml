<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:util="clr-namespace:TipCalculator;assembly=TipCalculator"
             mc:Ignorable="d"
             x:Class="TipCalculator.MainPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <util:ConvertidorMoneda x:Key="convertidor"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout>
        <!-- Place new controls here -->
        <TableView Intent="Form">
            <TableSection Title="Datos">
                <EntryCell x:Name="txtTotal"
                        Label="Total"
                        Placeholder="¢0.0"
                        Text="{Binding Total}"
                        Keyboard="Numeric"/>
                <EntryCell x:Name="txtPropina"
                           Label="Propina (%)"
                           Text="{Binding Propina}"
                           Keyboard="Numeric"/>
                <EntryCell x:Name="txtPersonas"
                           Label="# de personas"
                           Text="{Binding noPersonas}"
                           Keyboard="Numeric"/>
            </TableSection>
            <TableSection>
                <ViewCell>
                    <ViewCell.View>
                        <StackLayout>
                            <Button x:Name="btnCalcular"
                                    Text="Calcular"
                                    Command="{Binding OperationsCommand}">
                                <!--Clicked="BtnCalcular_OnClicked"-->
                            </Button>
                        </StackLayout>
                    </ViewCell.View>
                </ViewCell>
            </TableSection>
            <TableSection Title="Resultado">
                <TextCell x:Name="lblPropina"
                          Text="Propina: "
                          Detail="{Binding TotalPropina, Converter={StaticResource convertidor}}"/>
                <TextCell x:Name="lblTotal"
                          Text="Total: "
                          Detail="{Binding TotalConPropina, Converter={StaticResource convertidor}}"/>
                <TextCell x:Name="lblPropinaxPersona"
                          Text="Propina por persona: "
                          Detail="{Binding PropinaPorPersona, Converter={StaticResource convertidor}}"/>
                <TextCell x:Name="lblTotalxPersona"
                          Text="Total por persona: "
                          Detail="{Binding TotalPorPersona, Converter={StaticResource convertidor}}"/>
            </TableSection>
        </TableView>
    </StackLayout>

</ContentPage>
